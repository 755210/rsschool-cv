# Andrey Shcherbakov
* __Email__ 755210@gmail.com
* __Discord__ Andrey (@755210)
## Brief Self-Introduction
My goals - professional development
## Skils
Tehnological platform 1C: Enterprise
## Work Experience
Development according to customer requirements
## Code Examples
``` 1C: Enterprise
// Процедура - Сравнить все
// Сравнивает файлы, по путям из двух таблиц, если, кроме вторых строк файлы различны, отмечает различие, 
// если переменная ОткрытьWinMerge = Истина, открывает программу WinMerge для сравнения каждого файла  
&НаКлиенте
Процедура СравнитьВсе()	
	сч =0;	
	Пока Истина Цикл		
		строки1 = ТЗ_Старая.НайтиСтроки(новый структура("Номер",сч));
		строки2 = ТЗ_Новая.НайтиСтроки(новый структура("Номер",сч)); 		
		Если строки1.Количество() > 0 ИЛИ строки2.Количество() > 0  Тогда			
			Макс = ?(строки1.Количество() > строки2.Количество(), строки1.Количество(), строки2.Количество());			
			Для сч1 = 0 По Макс-1 Цикл				
				Если строки1.Количество() > 0 И строки1.Количество() > сч1 Тогда				
					Стр1 = строки1[сч1].Путь;	
				Иначе
					Стр1 = "";
				КонецЕсли;				
				Если строки2.Количество() > 0 И строки2.Количество() > сч1 Тогда				
					Стр2 = строки2[сч1].Путь;	
				Иначе
					Стр2 = "";
				КонецЕсли;				
				Если Стр1 = "" И Стр2 = "" Тогда
					Продолжить;			
				КонецЕсли;				
				Если ОткрытьWinMerge Тогда					
					СтрокаКомады = КаталогWinMerge + " " + Стр1 + " " + Стр2; 
					ЗапуститьПриложение(СтрокаКомады);					
				Иначе						
					Если Стр1 = "" ИЛИ Стр2 = "" Тогда
					 	ОтметитьРазличие(строки1, строки2, Стр1, Стр2);				
						Продолжить;
					КонецЕсли;					
					Текст1 = Новый ЧтениеТекста(Стр1);
					Текст2 = Новый ЧтениеТекста(Стр2);
					СтрокаТекст1 = "";
					СтрокаТекст2 = "";
					счСтрок = 0;
					Пока СтрокаТекст1 <> Неопределено И СтрокаТекст2 <> Неопределено  Цикл						
						СтрокаТекст1 = Текст1.ПрочитатьСтроку();
						СтрокаТекст2 = Текст2.ПрочитатьСтроку();						
						Если (СтрокаТекст1 <> СтрокаТекст2) И счСтрок <> 1 Тогда					
							ОтметитьРазличие(строки1, строки2, Стр1, Стр2);
							Прервать;
						КонецЕсли;						
						счСтрок = счСтрок + 1;						
					КонецЦикла;										
				КонецЕсли;							
			КонецЦикла;			
		Иначе
			Прервать;
		КонецЕсли;		
	    сч = сч + 1;		
	КонецЦикла;				
КонецПроцедуры
```
## Education
Novosibirsk State Technical University - (Computer Sience) Software Engineering
## English Language 
Elementary